const element = document.querySelectorAll('#one')
element.forEach(el => console.log(el))
//aqui é um querySelectorAll, isso quer dizer que ele pegará todas as tag que tanham a assinatura one como classe ou id
//é muito bom para pegar varios elementos diferente, detalhe que ele será retornado como uma lista para o usuario 
//e suas informções podem ser pega em algum lça ou acesso direto
