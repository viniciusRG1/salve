#include <iostream>
#include <bits/stdc++.h>

using namespace std;
int MAX = 3;

int main(){

    int MxM[3][3];
      for(int M = 0; M < MAX; M++){
       for(int N = 0; N < MAX; N++){
           cout << "escreva seu o número da matriz" << endl;
           cin >> MxM[M][N];

    }
    }
    for(int M = 0; M < MAX; M++){
       for(int N = 0; N < MAX; N++){
           cout << MxM[M][N]<< endl;

    }
    cout << endl;
    }

return 0;
}
